<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.login_module">

    <application>
        <activity android:name=".activity.StartActivity"></activity>

        <activity android:name=".activity.LoginActivity" />

        <activity android:name=".activity.ModifyLoginPasswordActivity" />

        <activity android:name=".activity.ModifyPayPasswordActivity" />

        <activity android:name=".activity.RegisterActivity" />

        <activity android:name=".activity.InformationActivity" />

        <activity android:name=".activity.SelectPassActivity" />

        <activity android:name=".activity.ForgetLoginPassActivity" />

        <activity android:name=".activity.ForgetPayPassActivity" />

    </application>

</manifest>