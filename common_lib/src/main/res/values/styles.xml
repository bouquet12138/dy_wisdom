<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="QMUI.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/app_theme_color</item>
        <item name="colorPrimaryDark">@color/app_title_color</item>
        <item name="colorAccent">@color/app_theme_color</item>
        <item name="android:textViewStyle">@style/AppMyTextViewStyle</item>
    </style>

    <style name="AppMyTextViewStyle">
        <item name="android:textColor">@color/app_text_color</item>
        <item name="android:textSize">16dp</item>
    </style>

</resources>
